// take a string, the button label and return a element

/**
 * @param {string} buttonName
 * @returns {Element}
 */
function button(buttonName) {
  return `Button: ${buttonName}`;
}

module.exports = button;
